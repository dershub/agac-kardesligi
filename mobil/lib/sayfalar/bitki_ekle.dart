import 'package:flutter/material.dart';

import '../gerecler/yazilar.dart';

class BitkiEkle extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(yazi['tr']['bitki-ekle']),
      ),
      body: Column(
        children: [
          Row(
            children: [],
          ),
        ],
      ),
    );
  }
}
